// Package scale provides functions to estimate
// musical scales when tonics are given
package scale

// Scale takes a tonic and an interval
// as params and returns its scale
func Scale(tonic string, interval string) []string {
	return []string{""}
}
